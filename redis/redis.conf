# Minimal redis.conf for local development with keyspace notifications enabled
bind 0.0.0.0
protected-mode no
port 6379
save 900 1
save 300 10
save 60 10000
appendonly no
# Enable keyspace events for all event types (K: Keyspace, E: Keyevent, A: All)
notify-keyspace-events "KEA"
